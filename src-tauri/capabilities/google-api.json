{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "google-api",
  "description": "Capability for Google API access with stronghold secrets",
  "windows": ["main"],
  "permissions": [
    "stronghold:default"
  ]
}
